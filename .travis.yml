language: node_js
node_js:
    - "4.x"
sudo: false
before_install: npm install -g grunt-cli
install: npm install
services:
    - mongodb
after_script: "npm install coveralls && cat ./lcov.info | coveralls"
