const should = require('should');
const store = require('../../../server/db/store');
const Model = require('../../../server/db/model');

describe('Model', () => {
    class MockModel extends Model{}
});
